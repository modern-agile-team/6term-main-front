import SaveToken from '@/components/molecules/login-modal/SaveToken';

const RedirectUri = () => {
  return (
    <div>
      <SaveToken provider="naver" />
    </div>
  );
};

export default RedirectUri;
